# Copy this into a consuming repo as `.github/workflows/pr-review.yml`.
# Replace <owner> with the org/user that hosts the agents repo.
#
# Default model is github/openai/gpt-5-mini via GitHub Models — the built-in
# GITHUB_TOKEN authenticates it, so no API key is needed, but the token needs
# `models: read` (below) and paid/enabled GitHub Models ("custom" tier). For a
# free model use `model: github/openai/gpt-4.1` (note its ~8k token cap), or see
# the Anthropic block at the bottom.
name: PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  models: read

jobs:
  review:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4 # so the agent can see this repo's .flue/ overrides

      - uses: <owner>/agents/actions/pr-review@main # @main = always-latest skills/prompts; pin @v1 for reproducible
        with:
          pr-number: ${{ github.event.pull_request.number }}
          # Optional:
          # model: github/openai/gpt-5     # larger/stronger, also paid
          # model: github/openai/gpt-4.1   # free tier, ~8k token cap
          # override-mode: merge           # or: replace

      # --- Or review with Anthropic instead of GitHub Models -----------------
      # Drop `models: read` above, add ANTHROPIC_API_KEY as a repo secret, and
      # use this step instead:
      #
      # - uses: <owner>/agents/actions/pr-review@main
      #   with:
      #     pr-number: ${{ github.event.pull_request.number }}
      #     model: anthropic/claude-sonnet-4-6
      #     anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
